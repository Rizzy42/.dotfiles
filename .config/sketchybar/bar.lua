local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  topmost = "everything",
  height = 40,
  font_smoothing = true,
  color = colors.transparent,
  padding_right = 2,
  padding_left = 2
})
